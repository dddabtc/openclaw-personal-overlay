# Windows payload example

This directory is a minimal example payload for `scripts/windows/apply-windows.ps1`.

It shows the expected layout:

- `manifest.json`
- files referenced by `manifest.json`

The example files here are placeholders for structure only.
