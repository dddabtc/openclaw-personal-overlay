From 9371986d8e97ef38d1e0ceb1abf73d08ac814981 Mon Sep 17 00:00:00 2001
From: subagent-bot <subagent-bot@example.com>
Date: Sun, 8 Mar 2026 06:27:28 -0300
Subject: [PATCH 7/8] feat(exec): add session maxOutputBytes policy

---
 src/agents/bash-tools.exec-types.ts | 11 ++++++
 src/agents/bash-tools.exec.ts       | 58 ++++++++++++++++++++++++++++-
 src/agents/pi-tools.ts              |  3 ++
 src/config/schema.ts                | 25 -------------
 src/config/types.openclaw.ts        |  4 ++
 src/config/zod-schema.ts            |  7 ++++
 6 files changed, 81 insertions(+), 27 deletions(-)

diff --git a/src/agents/bash-tools.exec-types.ts b/src/agents/bash-tools.exec-types.ts
index 5f62df254..f0ded01ff 100644
--- a/src/agents/bash-tools.exec-types.ts
+++ b/src/agents/bash-tools.exec-types.ts
@@ -6,6 +6,16 @@ export type ExecMainSessionPolicy = {
   forbidLongExec?: boolean;
   requireBackgroundForExec?: boolean;
   maxExecTimeoutSec?: number;
+  maxOutputBytes?: number;
+};
+
+export type ExecSubagentSessionPolicy = {
+  maxOutputBytes?: number;
+};
+
+export type ExecSessionPolicies = {
+  main?: ExecMainSessionPolicy;
+  subagent?: ExecSubagentSessionPolicy;
 };
 
 export type ExecToolDefaults = {
@@ -34,6 +44,7 @@ export type ExecToolDefaults = {
   notifyOnExitEmptySuccess?: boolean;
   cwd?: string;
   mainSessionPolicy?: ExecMainSessionPolicy;
+  sessionPolicies?: ExecSessionPolicies;
 };
 
 export type ExecElevatedDefaults = {
diff --git a/src/agents/bash-tools.exec.ts b/src/agents/bash-tools.exec.ts
index a3aca1df3..af3f3e827 100644
--- a/src/agents/bash-tools.exec.ts
+++ b/src/agents/bash-tools.exec.ts
@@ -32,6 +32,7 @@ import {
 import type {
   ExecElevatedDefaults,
   ExecMainSessionPolicy,
+  ExecSessionPolicies,
   ExecToolDefaults,
   ExecToolDetails,
 } from "./bash-tools.exec-types.js";
@@ -50,6 +51,7 @@ export type { BashSandboxConfig } from "./bash-tools.shared.js";
 export type {
   ExecElevatedDefaults,
   ExecMainSessionPolicy,
+  ExecSessionPolicies,
   ExecToolDefaults,
   ExecToolDetails,
 } from "./bash-tools.exec-types.js";
@@ -174,6 +176,49 @@ function isSshLikeExecCommand(command: string): boolean {
   return /^(?:ssh|scp|sftp)\b/i.test(trimmed);
 }
 
+function resolveSessionMaxOutputBytes(params: {
+  sessionPolicies?: ExecSessionPolicies;
+  mainSessionPolicy?: ExecMainSessionPolicy;
+  sessionKey?: string;
+}) {
+  const parsed = parseAgentSessionKey(params.sessionKey);
+  const rest = (parsed?.rest ?? "").toLowerCase();
+  const isSubagent = rest.startsWith("subagent:");
+  const raw = isSubagent
+    ? params.sessionPolicies?.subagent?.maxOutputBytes
+    : params.mainSessionPolicy?.maxOutputBytes ?? params.sessionPolicies?.main?.maxOutputBytes;
+  const fallback = isSubagent ? 0 : 102_400;
+  if (typeof raw !== "number" || !Number.isFinite(raw) || raw < 0) {
+    return fallback;
+  }
+  return Math.floor(raw);
+}
+
+function applyMaxOutputBytes(text: string, maxBytes: number): string {
+  if (maxBytes <= 0) {
+    return text;
+  }
+  const source = String(text ?? "");
+  const size = Buffer.byteLength(source, "utf8");
+  if (size <= maxBytes) {
+    return source;
+  }
+  const suffix = `\n…(truncated, ${size} bytes → ${maxBytes} bytes. Use sub-agent for full output.)`;
+  const budget = Math.max(0, maxBytes - Buffer.byteLength(suffix, "utf8"));
+  let low = 0;
+  let high = source.length;
+  while (low < high) {
+    const mid = Math.ceil((low + high) / 2);
+    const candidate = source.slice(0, mid);
+    if (Buffer.byteLength(candidate, "utf8") <= budget) {
+      low = mid;
+    } else {
+      high = mid - 1;
+    }
+  }
+  return `${source.slice(0, low)}${suffix}`;
+}
+
 export function createExecTool(
   defaults?: ExecToolDefaults,
   // oxlint-disable-next-line typescript/no-explicit-any
@@ -282,6 +327,11 @@ export function createExecTool(
 
       const maxOutput = DEFAULT_MAX_OUTPUT;
       const pendingMaxOutput = DEFAULT_PENDING_MAX_OUTPUT;
+      const sessionOutputMaxBytes = resolveSessionMaxOutputBytes({
+        sessionPolicies: defaults?.sessionPolicies,
+        mainSessionPolicy,
+        sessionKey: defaults?.sessionKey,
+      });
       const warnings: string[] = [];
       let execCommandOverride: string | undefined;
       const backgroundRequested = params.background === true;
@@ -619,18 +669,22 @@ export function createExecTool(
               reject(new Error(outcome.reason ?? "Command failed."));
               return;
             }
+            const aggregated = applyMaxOutputBytes(
+              outcome.aggregated || "(no output)",
+              sessionOutputMaxBytes,
+            );
             resolve({
               content: [
                 {
                   type: "text",
-                  text: `${getWarningText()}${outcome.aggregated || "(no output)"}`,
+                  text: `${getWarningText()}${aggregated}`,
                 },
               ],
               details: {
                 status: "completed",
                 exitCode: outcome.exitCode ?? 0,
                 durationMs: outcome.durationMs,
-                aggregated: outcome.aggregated,
+                aggregated,
                 cwd: run.session.cwd,
               },
             });
diff --git a/src/agents/pi-tools.ts b/src/agents/pi-tools.ts
index 4c365df80..49e6a6299 100644
--- a/src/agents/pi-tools.ts
+++ b/src/agents/pi-tools.ts
@@ -155,6 +155,8 @@ function resolveExecConfig(params: { cfg?: OpenClawConfig; agentId?: string }) {
     notifyOnExitEmptySuccess:
       agentExec?.notifyOnExitEmptySuccess ?? globalExec?.notifyOnExitEmptySuccess,
     applyPatch: agentExec?.applyPatch ?? globalExec?.applyPatch,
+    mainSessionPolicy: cfg?.sessionPolicies?.main,
+    sessionPolicies: cfg?.sessionPolicies,
   };
 }
 
@@ -418,6 +420,7 @@ export function createOpenClawCodingTools(options?: {
     notifyOnExitEmptySuccess:
       options?.exec?.notifyOnExitEmptySuccess ?? execConfig.notifyOnExitEmptySuccess,
     mainSessionPolicy: options?.exec?.mainSessionPolicy ?? execConfig.mainSessionPolicy,
+    sessionPolicies: options?.exec?.sessionPolicies ?? execConfig.sessionPolicies,
     sandbox: sandbox
       ? {
           containerName: sandbox.containerName,
diff --git a/src/config/schema.ts b/src/config/schema.ts
index a445c9508..0bce1698c 100644
--- a/src/config/schema.ts
+++ b/src/config/schema.ts
@@ -12,31 +12,6 @@ export type ConfigSchema = ReturnType<typeof OpenClawSchema.toJSONSchema>;
 
 type JsonSchemaNode = Record<string, unknown>;
 
-export type ConfigSchemaResponse = {
-  schema: ConfigSchema;
-  uiHints: ConfigUiHints;
-  version: string;
-  generatedAt: string;
-};
-
-export type PluginUiMetadata = {
-  id: string;
-  name?: string;
-  description?: string;
-  configUiHints?: Record<
-    string,
-    Pick<ConfigUiHint, "label" | "help" | "advanced" | "sensitive" | "placeholder">
-  >;
-  configSchema?: JsonSchemaNode;
-};
-
-export type ChannelUiMetadata = {
-  id: string;
-  label?: string;
-  description?: string;
-  configSchema?: JsonSchemaNode;
-  configUiHints?: Record<string, ConfigUiHint>;
-};
 
 const GROUP_LABELS: Record<string, string> = {
   wizard: "Wizard",
diff --git a/src/config/types.openclaw.ts b/src/config/types.openclaw.ts
index 38efa88a9..b846c545d 100644
--- a/src/config/types.openclaw.ts
+++ b/src/config/types.openclaw.ts
@@ -125,6 +125,10 @@ export type OpenClawConfig = {
       forbidLongExec?: boolean;
       requireBackgroundForExec?: boolean;
       maxExecTimeoutSec?: number;
+      maxOutputBytes?: number;
+    };
+    subagent?: {
+      maxOutputBytes?: number;
     };
   };
 };
diff --git a/src/config/zod-schema.ts b/src/config/zod-schema.ts
index 7a541674c..5803d160f 100644
--- a/src/config/zod-schema.ts
+++ b/src/config/zod-schema.ts
@@ -443,6 +443,13 @@ export const OpenClawSchema = z
             forbidLongExec: z.boolean().optional(),
             requireBackgroundForExec: z.boolean().optional(),
             maxExecTimeoutSec: z.number().int().positive().optional(),
+            maxOutputBytes: z.number().int().nonnegative().optional(),
+          })
+          .strict()
+          .optional(),
+        subagent: z
+          .object({
+            maxOutputBytes: z.number().int().nonnegative().optional(),
           })
           .strict()
           .optional(),
-- 
2.25.1

