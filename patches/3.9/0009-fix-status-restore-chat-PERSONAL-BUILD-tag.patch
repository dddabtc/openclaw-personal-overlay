From d50f739cbd93e590786694ceb4200f41f749ff38 Mon Sep 17 00:00:00 2001
From: subagent-bot <subagent-bot@example.com>
Date: Wed, 1 Apr 2026 22:40:00 -0300
Subject: [PATCH 09/10] fix(status): restore chat PERSONAL BUILD tag

---
 src/status/status-message.ts | 8 ++++++++
 1 file changed, 8 insertions(+)

diff --git a/src/status/status-message.ts b/src/status/status-message.ts
--- a/src/status/status-message.ts
+++ b/src/status/status-message.ts
@@ -826,8 +826,13 @@ export function buildStatusMessage(args: StatusArgs): string {
         showFallbackAuth ? ` · 🔑 ${activeAuthLabelValue}` : ""
       } (${fallbackState.reason ?? "selected model unavailable"})`
     : null;
+  // PERSONAL BUILD metadata injected at build time
+  const PERSONAL_BUILD_DATE = "__PERSONAL_BUILD_DATE__";
+  const PERSONAL_BUILD_REPO = "https://github.com/dddabtc";
   const commit = resolveCommitHash({ moduleUrl: import.meta.url });
   const versionLine = `🦞 OpenClaw ${VERSION}${commit ? ` (${commit})` : ""}`;
+  // PERSONAL BUILD: Add build tag line
+  const personalBuildLine = `🏷 PERSONAL BUILD · ${PERSONAL_BUILD_DATE}(UTC)\n${PERSONAL_BUILD_REPO}`;
   const usagePair = formatUsagePair(inputTokens, outputTokens);
   const cacheLine = formatCacheLine(inputTokens, cacheRead, cacheWrite);
   const costLine = costLabel ? `💵 Cost: ${costLabel}` : null;
@@ -838,6 +843,7 @@ export function buildStatusMessage(args: StatusArgs): string {
 
   return [
     versionLine,
+    personalBuildLine,
     args.timeLine,
     modelLine,
     configuredFallbacksLine,
