From d7d5c6f6ceb4b2ad1643d9a34947e9a88f6e4cb8 Mon Sep 17 00:00:00 2001
From: dda <dddabtc@users.noreply.github.com>
Date: Sun, 12 Apr 2026 02:33:01 +0000
Subject: [PATCH 10/10] PERSONAL BUILD: update test expectations for 21600 default

---
 .../openclaw-tools.subagents.sessions-spawn.model.test.ts     | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/src/agents/openclaw-tools.subagents.sessions-spawn.model.test.ts b/src/agents/openclaw-tools.subagents.sessions-spawn.model.test.ts
index c884e18d..83ffc8ae 100644
--- a/src/agents/openclaw-tools.subagents.sessions-spawn.model.test.ts
+++ b/src/agents/openclaw-tools.subagents.sessions-spawn.model.test.ts
@@ -237,13 +237,13 @@ describe("subagent spawn model + thinking plan", () => {
     ).toBe(2);
   });
 
-  it("falls back to 0 when config omits the timeout", () => {
+  it("falls back to 21600 (6 hours) when config omits the timeout", () => {
     expect(
       resolveConfiguredSubagentRunTimeoutSeconds({
         cfg: createConfig({
           agents: { defaults: { subagents: { maxConcurrent: 8 } } },
         }),
       }),
-    ).toBe(0);
+    ).toBe(21600);
   });
 });
-- 
2.25.1

